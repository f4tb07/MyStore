using Microsoft.EntityFrameworkCore;
using Domain;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace DAL
{
    //public class CustomerAddressConfiguration : IEntityTypeConfiguration<CustomersAddress>
    //{
    //    public void Configure(EntityTypeBuilder<CustomersAddress> builder)
    //    {
    //        builder.ToTable("CustomerAddressTBL", "Customer");
    //        builder.Property(CA=>CA.State).HasMaxLength(20);
    //        builder.Property(CA => CA.City).HasMaxLength(10);
    //        builder.Property(CA => CA.Address).HasMaxLength(50);
    //        builder.Property(CA => CA.PostalCode).HasMaxLength(10);
            

    //    }
    //}
}
