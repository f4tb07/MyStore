using Microsoft.EntityFrameworkCore;
using Domain;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace InternetstorDataAcsessLayer
{
    //public class CustomerConfiguration : IEntityTypeConfiguration<Customer>
    //{
    //    public void Configure(EntityTypeBuilder<Customer> builder)
    //    {
    //        builder.ToTable("CustomerTBL", "Customer");
    //        builder.Property(C=>C.Username).HasMaxLength(20);
    //        builder.Property(C => C.Password).HasMaxLength(20);
    //        builder.Property(C => C.Name).HasMaxLength(20);
    //        builder.Property(C => C.Family).HasMaxLength(20);
    //        builder.Property(C => C.Mobile).HasMaxLength(20);
    //        builder.Property(C => C.Email).HasMaxLength(50);


    //    }
    //}
}
